<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Attendance Management Web App</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', system-ui, sans-serif; }
    .scrollbar-thin::-webkit-scrollbar { height: 6px; }
    .scrollbar-thin::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
    .modal-backdrop { background: rgba(0,0,0,0.2); }
    .toast-enter { opacity: 0; transform: translateY(-10px); }
    .toast-enter-active { opacity: 1; transform: translateY(0); transition: all 0.3s; }
    .toast-exit { opacity: 1; }
    .toast-exit-active { opacity: 0; transition: opacity 0.3s; }
    .focus-ring:focus { outline: 2px solid #38bdf8; outline-offset: 2px; }
  </style>
</head>
<body class="bg-blue-50 min-h-screen flex flex-col">
  <header class="flex items-center justify-between px-4 py-3 bg-white shadow-sm sticky top-0 z-30">
    <div class="flex items-center gap-2">
      <button id="sidebarToggle" aria-label="Toggle sidebar" class="md:hidden text-blue-600 focus-ring">
        <svg width="28" height="28" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 7h20M4 14h20M4 21h20"/></svg>
      </button>
      <span class="text-2xl font-semibold text-blue-700 tracking-tight">Attendance</span>
    </div>
    <div class="flex items-center gap-4">
      <span id="currentDate" class="text-gray-600 text-sm"></span>
      <div class="w-8 h-8 rounded-full bg-blue-200 flex items-center justify-center">
        <span class="text-blue-700 font-bold" aria-label="User avatar">👤</span>
      </div>
    </div>
  </header>

  <div class="flex flex-1">
    <nav id="sidebar" class="bg-white shadow-md w-56 min-w-[12rem] flex-shrink-0 flex flex-col py-6 px-4 gap-2 transition-all duration-200
      md:translate-x-0 md:static fixed inset-y-0 left-0 z-40 -translate-x-full" aria-label="Main navigation">
      <button id="sidebarClose" class="md:hidden self-end mb-2 text-blue-600 focus-ring" aria-label="Close sidebar">
        <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M6 18L18 6"/></svg>
      </button>
      <a href="#" data-view="dashboard" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-blue-700 font-medium hover:bg-blue-100 focus-ring" aria-label="Dashboard">
        <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><circle cx="10" cy="10" r="8"/><path d="M10 4v6l4 2"/></svg>
        Dashboard
      </a>
      <a href="#" data-view="attendance" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-blue-700 font-medium hover:bg-blue-100 focus-ring" aria-label="Mark Attendance">
        <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="14" height="12" rx="2"/><path d="M8 10h4"/></svg>
        Mark Attendance
      </a>
      <a href="#" data-view="students" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-blue-700 font-medium hover:bg-blue-100 focus-ring" aria-label="Students">
        <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M10 12a4 4 0 1 0 0-8 4 4 0 0 0 0 8z"/><path d="M4 16v-1a4 4 0 0 1 8 0v1"/></svg>
        Students
      </a>
      <a href="#" data-view="export" class="nav-link flex items-center gap-2 px-3 py-2 rounded-lg text-blue-700 font-medium hover:bg-blue-100 focus-ring" aria-label="Export">
        <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 16v-4m0 0l-3 3m3-3l3 3"/><rect x="4" y="4" width="16" height="12" rx="2"/></svg>
        Export
      </a>
      <div class="flex-1"></div>
    </nav>

    <main id="mainContent" class="flex-1 px-4 py-6 md:ml-0 ml-0 transition-all duration-200">
      <section id="view-dashboard" class="view-section">
        <h2 class="text-xl font-semibold text-blue-700 mb-4">Dashboard</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
          <div class="bg-white rounded-xl shadow p-4 flex flex-col items-center">
            <span class="text-gray-500 text-sm">Total Students</span>
            <span id="cardTotal" class="text-2xl font-bold text-blue-700 mt-2">0</span>
          </div>
          <div class="bg-white rounded-xl shadow p-4 flex flex-col items-center">
            <span class="text-gray-500 text-sm">Present Today</span>
            <span id="cardPresent" class="text-2xl font-bold text-green-600 mt-2">0</span>
          </div>
          <div class="bg-white rounded-xl shadow p-4 flex flex-col items-center">
            <span class="text-gray-500 text-sm">Absent Today</span>
            <span id="cardAbsent" class="text-2xl font-bold text-red-500 mt-2">0</span>
          </div>
          <div class="bg-white rounded-xl shadow p-4 flex flex-col items-center">
            <span class="text-gray-500 text-sm">Avg Attendance %</span>
            <span id="cardAvg" class="text-2xl font-bold text-blue-700 mt-2">0%</span>
          </div>
        </div>
        <div class="bg-white rounded-xl shadow p-4 mb-4">
          <h3 class="text-lg font-medium text-blue-700 mb-2">Attendance Overview</h3>
          <svg id="dashboardChart" width="100%" height="80" aria-label="Attendance bar chart"></svg>
        </div>
        <div class="bg-white rounded-xl shadow p-4 mb-4">
          <label for="studentSelect" class="block text-sm font-medium text-gray-700 mb-2">Check Individual Student Performance</label>
          <select id="studentSelect" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-full max-w-xs mb-2">
            <option value="">Select Student</option>
          </select>
          <div id="studentPerformance" class="text-sm text-gray-700 mt-2"></div>
        </div>
      </section>

      <section id="view-attendance" class="view-section hidden">
        <h2 class="text-xl font-semibold text-blue-700 mb-4">Mark Attendance</h2>
        <div class="mb-2 text-xs text-gray-500 bg-blue-100 rounded p-2">
          <strong>Note:</strong> Attendance data is saved locally in your browser. Email notifications are sent via server.
        </div>
        <form id="attendanceForm" class="mb-4 flex flex-col sm:flex-row gap-4 items-center">
          <div>
            <label for="attendanceDate" class="block text-sm font-medium text-gray-700 mb-1">Date</label>
            <input type="date" id="attendanceDate" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-40" required>
          </div>
          <div>
            <label for="subjectSelect" class="block text-sm font-medium text-gray-700 mb-1">Subject</label>
            <select id="subjectSelect" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-60" required>
              <option value="">Select a subject</option>
              <option value="Engineering Mathematics-1">Engineering Mathematics-1</option>
              <option value="Engineering Physics & Chemistry">Engineering Physics & Chemistry</option>
              <option value="Problem Solving using C & C++">Problem Solving using C & C++</option>
              <option value="Effective Technical Communication-I">Effective Technical Communication-I</option>
              <option value="IDEA Lab Workshop">IDEA Lab Workshop</option>
              <option value="Universal Human Values">Universal Human Values</option>
              <option value="Problem Solving using C & C++ Lab">Problem Solving using C & C++ Lab</option>
              <option value="Innovative projects 1.0 (Phase-1)IIC">Innovative projects 1.0 (Phase-1)IIC</option>
              <option value="Yoga & Practice/NSS">Yoga & Practice/NSS</option>
              <option value="Extra & co curricular">Extra & co curricular</option>
            </select>
          </div>
          <button type="button" id="resetAttendance" class="ml-0 sm:ml-4 px-4 py-2 rounded-lg border border-blue-300 text-blue-700 bg-white hover:bg-blue-50 font-medium transition focus-ring" aria-label="Reset attendance">Reset</button>
          <button type="submit" class="ml-0 sm:ml-2 px-4 py-2 rounded-lg bg-blue-600 text-white font-semibold shadow hover:bg-blue-700 transition focus-ring" aria-label="Save attendance">Save</button>
          <button type="button" id="sendAbsenteeEmails" class="ml-0 sm:ml-2 px-4 py-2 rounded-lg bg-red-600 text-white font-semibold shadow hover:bg-red-700 transition focus-ring" aria-label="Send absentee emails">Send Absentee Emails</button>
        </form>
        <div class="overflow-x-auto scrollbar-thin">
          <table class="min-w-full bg-white rounded-xl shadow text-sm">
            <thead class="sticky top-0 bg-blue-100">
              <tr>
                <th class="px-4 py-2 text-left font-semibold text-blue-700">Roll No</th>
                <th class="px-4 py-2 text-left font-semibold text-blue-700">Name</th>
                <th class="px-4 py-2 text-center font-semibold text-blue-700">Present</th>
              </tr>
            </thead>
            <tbody id="attendanceTable" class="divide-y divide-gray-100">
              </tbody>
          </table>
        </div>
      </section>

      <section id="view-students" class="view-section hidden">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-xl font-semibold text-blue-700">Students</h2>
          <button id="addStudentBtn" class="px-4 py-2 rounded-lg bg-green-600 text-white font-semibold shadow hover:bg-green-700 transition focus-ring" aria-label="Add student">Add Student</button>
        </div>
        <div class="overflow-x-auto scrollbar-thin">
          <table class="min-w-full bg-white rounded-xl shadow text-sm">
            <thead class="sticky top-0 bg-blue-100">
              <tr>
                <th class="px-4 py-2 text-left font-semibold text-blue-700">Roll No</th>
                <th class="px-4 py-2 text-left font-semibold text-blue-700">Name</th>
                <th class="px-4 py-2 text-center font-semibold text-blue-700">Attendance %</th>
                <th class="px-4 py-2 text-center font-semibold text-blue-700">Actions</th>
              </tr>
            </thead>
            <tbody id="studentsTable" class="divide-y divide-gray-100">
              <% students.forEach(student => { %>
                <tr class="<%= /^\d+$/.test(student.roll) && parseInt(student.roll) % 2 === 0 ? 'bg-blue-50' : '' %>">
                  <td class="px-4 py-2"><%= student.roll %></td>
                  <td class="px-4 py-2"><%= student.name %></td>
                  <td class="px-4 py-2 text-center">0%</td>
                  <td class="px-4 py-2 text-center">
                    <button class="edit-btn px-2 py-1 rounded bg-blue-100 text-blue-700 hover:bg-blue-200 focus-ring" data-roll="<%= student.roll %>" aria-label="Edit <%= student.name %>">Edit</button>
                    <button class="delete-btn px-2 py-1 rounded bg-red-100 text-red-700 hover:bg-red-200 focus-ring" data-roll="<%= student.roll %>" aria-label="Delete <%= student.name %>">Delete</button>
                  </td>
                </tr>
              <% }); %>
              </tbody>
          </table>
        </div>
      </section>

      <section id="view-export" class="view-section hidden">
        <h2 class="text-xl font-semibold text-blue-700 mb-4">Export Attendance</h2>
        <form id="exportForm" class="flex flex-col sm:flex-row gap-4 items-center mb-6">
          <div>
            <label for="exportStart" class="block text-sm font-medium text-gray-700 mb-1">Start Date</label>
            <input type="date" id="exportStart" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-40" required>
          </div>
          <div>
            <label for="exportEnd" class="block text-sm font-medium text-gray-700 mb-1">End Date</label>
            <input type="date" id="exportEnd" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-40" required>
          </div>
          <button type="button" id="downloadCSV" class="px-4 py-2 rounded-lg bg-blue-600 text-white font-semibold shadow hover:bg-blue-700 transition focus-ring" aria-label="Download CSV">Download CSV</button>
        </form>
        <div class="text-gray-500 text-sm">Exports attendance data for all students in the selected date range.</div>
      </section>
    </main>
  </div>

  <div id="studentModal" class="fixed inset-0 z-50 flex items-center justify-center hidden" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
    <div class="modal-backdrop absolute inset-0"></div>
    <div class="bg-white rounded-xl shadow-lg p-6 w-full max-w-md relative z-10">
      <h3 id="modalTitle" class="text-lg font-semibold text-blue-700 mb-4">Add Student</h3>
      <form id="studentForm" class="flex flex-col gap-4">
        <div>
          <label for="modalRoll" class="block text-sm font-medium text-gray-700 mb-1">Roll No</label>
          <input type="text" id="modalRoll" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-full" required>
          <span id="rollHelp" class="text-xs text-gray-400">Unique number</span>
        </div>
        <div>
          <label for="modalName" class="block text-sm font-medium text-gray-700 mb-1">Name</label>
          <input type="text" id="modalName" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-full" required maxlength="32">
        </div>
        <div>
          <label for="modalEmail" class="block text-sm font-medium text-gray-700 mb-1">Email <span class="text-red-500">*</span></label>
          <input type="email" id="modalEmail" class="focus-ring rounded-lg border-gray-300 px-3 py-2 w-full" placeholder="student@example.com" required>
          <span class="text-xs text-gray-400">Required for absence notifications</span>
        </div>
        <div class="flex gap-2 mt-2">
          <button type="submit" class="px-4 py-2 rounded-lg bg-blue-600 text-white font-semibold shadow hover:bg-blue-700 transition focus-ring" aria-label="Save student">Save</button>
          <button type="button" id="modalCancel" class="px-4 py-2 rounded-lg border border-blue-300 text-blue-700 bg-white hover:bg-blue-50 font-medium transition focus-ring" aria-label="Cancel">Cancel</button>
        </div>
      </form>
    </div>
  </div>

  <div id="toastContainer" class="fixed top-4 right-4 z-50 flex flex-col gap-2"></div>

  <footer class="mt-auto py-3 px-4 text-center text-xs text-gray-400 bg-white border-t">
    &copy; 2025 Attendance Web App &mdash; Made by Anuj Kumar Thakur (Project by Anuj Kumar Thakur).
  </footer>

  <script>
    // Make students data available to JavaScript
    window.studentsData = <%- JSON.stringify(students) %>;
  </script>
  <script src="/script.js"></script>
</body>
</html>